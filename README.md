# Data-mining
A project for Social Media Mining

# Install library: PowerShell
1. Create Virtual Environment: python -m venv .venv
    Recheck after: ls .venv\Scripts (You should be seeing Activate.ps1, python.exe, pip.exe)
    Active: .\.venv\Scripts\Activate.ps1
2. python -m pip install pandas
3. pip install --upgrade pip
4. pip install pandas numpy scikit-learn torch transformers tqdm

# Run model